spring:
   application:
      name: service-emaillist # msa 등록할 이름 / log파일 이름
   profiles:
      active: development
   
---  
spring:
   config:
      activate:
         on-profile: development
      import: optional:configserver:http://localhost:7777
      
---  
spring:
   config:
      activate:
         on-profile: production   
      import: optional:configserver:http://192.168.0.69:7777
      
